<% content_for :title, "Edit Job Lead - #{@job_lead.title}" %>

<div class="md:w-2/3 w-full mx-auto">
  <div class="mb-4">
    <%= link_to :back, class: 'btn btn-sm btn-secondary btn-muted gap-1.5' do %>
      <%= inline_svg_tag 'icons/arrow-left.svg', class: 'size-3.5 shrink-0' %>
      <span>Back</span>
    <% end %>
  </div>

  <h1 class="heading mb-6">Edit Job Lead</h1>

  <%= render 'form', job_lead: @job_lead %>
</div>
