<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style>
      h1, p, a {
        margin: 0;
        padding: 0;
        font: inherit;
        color: inherit;
        text-decoration: none;
      }

      a {
        cursor: pointer;
      }

      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
        font-family: 'Aspekta', ui-sans-serif, sans-serif;
        background-color: #ffffff;
        color: #1a1a1a;
        padding: 1rem;
        border-radius: 1rem;
        border: 1px solid #d4d4d8;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
      }

      .email-heading {
        font-size: 1.75rem;
        font-weight: 700;
        color: #0a0a0a;
        letter-spacing: -0.015em;
        margin-bottom: 1rem;
      }

      .email-text {
        font-size: 1rem;
        line-height: 1.5;
        color: #333333;
        margin-bottom: 1.5rem;
      }

      .email-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        background-color: #059669;
        color: white;
        font-size: 0.875rem;
        font-weight: 500;
        border-radius: 0.5rem;
        text-decoration: none;
        transition: background-color 150ms ease-in-out;
      }

      .email-button:hover {
        background-color: #047857;
      }

      .email-footer {
        margin-top: 1.5rem;
        font-size: 0.875rem;
        color: #666666;
        line-height: 1.4;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
