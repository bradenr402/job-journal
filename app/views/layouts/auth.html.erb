<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
  </head>

  <body class="bg-neutral-200/80 dark:bg-black text-normal">
    <%= render 'components/flash', rounded: false %>

    <div class="min-h-svh grid place-items-center">
      <main class="flex items-center justify-center h-full px-4 pt-12 pb-32">
        <div class="max-w-lg mx-auto sm:w-7/8 w-full">
          <div class="flex justify-center mb-8">
            <%= link_to root_path, class: 'flex items-center gap-1.5 transition-opacity duration-80 hover:opacity-80' do %>
              <%= icon 'note-filled', class: 'size-7 shrink-0 text-primary-600' %>
              <span class="text-2xl font-extrabold tracking-tight text-normal">JobJournal</span>
            <% end %>
          </div>

          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
